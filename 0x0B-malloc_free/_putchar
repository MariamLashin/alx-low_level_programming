#include "main.h"
#include <unistd.h>

/**
* _putchar : function that writes the character c to stdout.
* @c : char. to be print.
* return : 1 on success , -1 on error.
*/

int _putchar(char c)
{
return (write(1, &c, 1));
}

